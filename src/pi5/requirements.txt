# Raspberry Pi 5 — supervisor runtime (Python 3).
#
#   python3 -m pip install -r src/pi5/requirements.txt
#
# On Pi 5 use the lgpio backend; legacy RPi.GPIO does not work reliably on Pi 5.

lgpio>=0.2                              # GPIO (Pi 5 backend)
gpiozero>=2.0                           # high-level GPIO over lgpio
pyserial>=3.5                          # TF03 lidar + Pico UART link
smbus2>=0.4                            # I2C bus (IMU)
adafruit-circuitpython-bno055>=5.4     # DFRobot Gravity BNO055 IMU (SEN0253)
# adafruit-circuitpython-bmp280>=3.2   # BMP280 baro on the same board (unused on a ground turret)
# OLED moved to the Pico (debug). The Pi 5 drives the armed/safe LED (GPIO) and
# the 10.1" touch GUI; add the chosen GUI toolkit here when picked.
