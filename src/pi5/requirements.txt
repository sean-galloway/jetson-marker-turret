# Raspberry Pi 5 — supervisor runtime (Python 3).
#
#   python3 -m pip install -r src/pi5/requirements.txt
#
# On Pi 5 use the lgpio backend; legacy RPi.GPIO does not work reliably on Pi 5.

lgpio>=0.2                              # GPIO (Pi 5 backend)
gpiozero>=2.0                           # high-level GPIO over lgpio
pyserial>=3.5                          # TF03 lidar + Pico UART link (IMU comes via Pico)
# IMU (BNO055) and the debug OLED now live on the Pico, not the Pi 5.
# The Pi 5 drives the armed/safe LED (GPIO) and the touch GUI; add the chosen
# GUI toolkit here when picked.
